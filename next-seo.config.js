const title = "Everyware";
const description = "Online shop";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    siteName: title,
  },
};

export default nextSeoConfig;
