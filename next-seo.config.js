const title = "Online shop";
const description = "Online shop with clothing";

const nextSeoConfig = {
  title,
  description,
  openGraph: {
    title,
    description,
    siteName: title,
  },
};

export default nextSeoConfig;
